<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
        integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
    <title>Encrypt URL</title>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
            <a class="navbar-brand" href="#">Encrypt URL</a>
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link" href="#">Home</a>
                </li>
            </ul>
        </div>
    </nav>
    <div class="container">
        <div class="d-flex flex-column justify-content-center">
            <form class="mt-3" action="/decryptUrl" method="POST">
                <div class="form-group">
                    <input hidden type="text" value="<%= encryptedUrl %>" class="form-control" name="text">
                </div>
                <div class="form-group">
                    <label for="key">Key to Decrypt:</label>
                    <input type="password" class="form-control" name="key">
                    <small class="form-text text-muted">Only individuals with this key can decrypt your URL.</small>
                </div>
                <button type="submit" class="btn btn-outline-primary">Decrypt and redirect</button>
            </form>
        </div>

    </div>
</body>

</html>
